/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopeAttachmentsEnvelopeAttachmentUpdate } from "../../funcs/envelopeAttachmentsEnvelopeAttachmentUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeAttachmentUpdateRequest$inboundSchema,
};

export const tool$envelopeAttachmentsEnvelopeAttachmentUpdate: ToolDefinition<
  typeof args
> = {
  name: "envelope-attachments-envelope-attachment-update",
  description: `Update attachment

Update an existing attachment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopeAttachmentsEnvelopeAttachmentUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
