/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { folderFolderCreateFolder } from "../../funcs/folderFolderCreateFolder.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.FolderCreateFolderRequest$inboundSchema,
};

export const tool$folderFolderCreateFolder: ToolDefinition<typeof args> = {
  name: "folder-folder-create-folder",
  description: `Create new folder

Creates a new folder in your team`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await folderFolderCreateFolder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
