/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopeItemsEnvelopeItemDelete } from "../../funcs/envelopeItemsEnvelopeItemDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeItemDeleteRequest$inboundSchema,
};

export const tool$envelopeItemsEnvelopeItemDelete: ToolDefinition<typeof args> =
  {
    name: "envelope-items-envelope-item-delete",
    description: `Delete envelope item

Delete an envelope item from an envelope`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await envelopeItemsEnvelopeItemDelete(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
