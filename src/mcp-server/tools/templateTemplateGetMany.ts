/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { templateTemplateGetMany } from "../../funcs/templateTemplateGetMany.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.TemplateGetManyRequest$inboundSchema,
};

export const tool$templateTemplateGetMany: ToolDefinition<typeof args> = {
  name: "template-template-get-many",
  description: `Get multiple templates

Retrieve multiple templates by their IDs`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await templateTemplateGetMany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
