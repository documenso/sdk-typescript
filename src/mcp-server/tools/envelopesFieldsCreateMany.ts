/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopesFieldsCreateMany } from "../../funcs/envelopesFieldsCreateMany.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeFieldCreateManyRequest$inboundSchema,
};

export const tool$envelopesFieldsCreateMany: ToolDefinition<typeof args> = {
  name: "envelopes-fields-create-many",
  description: `Create envelope fields

Create multiple fields for an envelope`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopesFieldsCreateMany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
