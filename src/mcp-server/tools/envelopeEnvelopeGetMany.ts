/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopeEnvelopeGetMany } from "../../funcs/envelopeEnvelopeGetMany.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeGetManyRequest$inboundSchema,
};

export const tool$envelopeEnvelopeGetMany: ToolDefinition<typeof args> = {
  name: "envelope-envelope-get-many",
  description: `Get multiple envelopes

Retrieve multiple envelopes by their IDs`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopeEnvelopeGetMany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
