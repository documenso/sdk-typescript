/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentDocumentDownloadBeta } from "../../funcs/documentDocumentDownloadBeta.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DocumentDownloadBetaRequest$inboundSchema,
};

export const tool$documentDocumentDownloadBeta: ToolDefinition<typeof args> = {
  name: "document-document-download-beta",
  description: `Download document (beta)

Get a pre-signed download URL for the original or signed version of a document`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentDocumentDownloadBeta(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
