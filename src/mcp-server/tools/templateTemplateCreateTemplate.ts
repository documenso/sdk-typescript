/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { templateTemplateCreateTemplate } from "../../funcs/templateTemplateCreateTemplate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.TemplateCreateTemplateRequest$inboundSchema,
};

export const tool$templateTemplateCreateTemplate: ToolDefinition<typeof args> =
  {
    name: "template-template-create-template",
    description: `Create template

Create a new template`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await templateTemplateCreateTemplate(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
