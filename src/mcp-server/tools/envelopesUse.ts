/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopesUse } from "../../funcs/envelopesUse.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeUseRequest$inboundSchema,
};

export const tool$envelopesUse: ToolDefinition<typeof args> = {
  name: "envelopes-use",
  description: `Use envelope

Create a document envelope from a template envelope. Upload custom files to replace the template PDFs and map them to specific envelope items using identifiers.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopesUse(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
