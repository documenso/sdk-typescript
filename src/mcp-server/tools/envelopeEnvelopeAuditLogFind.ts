/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopeEnvelopeAuditLogFind } from "../../funcs/envelopeEnvelopeAuditLogFind.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeAuditLogFindRequest$inboundSchema,
};

export const tool$envelopeEnvelopeAuditLogFind: ToolDefinition<typeof args> = {
  name: "envelope-envelope-audit-log-find",
  description: `Get envelope audit logs

Find audit logs based on a search criteria`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopeEnvelopeAuditLogFind(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
