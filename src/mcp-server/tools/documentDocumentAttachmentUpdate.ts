/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentDocumentAttachmentUpdate } from "../../funcs/documentDocumentAttachmentUpdate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DocumentAttachmentUpdateRequest$inboundSchema,
};

export const tool$documentDocumentAttachmentUpdate: ToolDefinition<
  typeof args
> = {
  name: "document-document-attachment-update",
  description: `Update attachment

Update an existing attachment`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentDocumentAttachmentUpdate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
