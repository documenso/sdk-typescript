/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopeFieldsEnvelopeFieldGet } from "../../funcs/envelopeFieldsEnvelopeFieldGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeFieldGetRequest$inboundSchema,
};

export const tool$envelopeFieldsEnvelopeFieldGet: ToolDefinition<typeof args> =
  {
    name: "envelope-fields-envelope-field-get",
    description: `Get envelope field

Returns an envelope field given an ID`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await envelopeFieldsEnvelopeFieldGet(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
