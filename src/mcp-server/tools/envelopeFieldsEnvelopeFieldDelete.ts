/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopeFieldsEnvelopeFieldDelete } from "../../funcs/envelopeFieldsEnvelopeFieldDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeFieldDeleteRequest$inboundSchema,
};

export const tool$envelopeFieldsEnvelopeFieldDelete: ToolDefinition<
  typeof args
> = {
  name: "envelope-fields-envelope-field-delete",
  description: `Delete envelope field

Delete an envelope field`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopeFieldsEnvelopeFieldDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
