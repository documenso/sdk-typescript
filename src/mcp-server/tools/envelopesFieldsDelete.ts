/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopesFieldsDelete } from "../../funcs/envelopesFieldsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeFieldDeleteRequest$inboundSchema,
};

export const tool$envelopesFieldsDelete: ToolDefinition<typeof args> = {
  name: "envelopes-fields-delete",
  description: `Delete envelope field

Delete an envelope field`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopesFieldsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
