/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopeFieldsEnvelopeFieldUpdateMany } from "../../funcs/envelopeFieldsEnvelopeFieldUpdateMany.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeFieldUpdateManyRequest$inboundSchema,
};

export const tool$envelopeFieldsEnvelopeFieldUpdateMany: ToolDefinition<
  typeof args
> = {
  name: "envelope-fields-envelope-field-update-many",
  description: `Update envelope fields

Update multiple envelope fields for an envelope`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopeFieldsEnvelopeFieldUpdateMany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
