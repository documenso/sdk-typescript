/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopesDuplicate } from "../../funcs/envelopesDuplicate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeDuplicateRequest$inboundSchema,
};

export const tool$envelopesDuplicate: ToolDefinition<typeof args> = {
  name: "envelopes-duplicate",
  description: `Duplicate envelope

Duplicate an envelope with all its settings`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopesDuplicate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
