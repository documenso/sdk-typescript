/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentDocumentAttachmentDelete } from "../../funcs/documentDocumentAttachmentDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DocumentAttachmentDeleteRequest$inboundSchema,
};

export const tool$documentDocumentAttachmentDelete: ToolDefinition<
  typeof args
> = {
  name: "document-document-attachment-delete",
  description: `Delete attachment

Delete an attachment from a document`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentDocumentAttachmentDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
