/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopeEnvelopeCertificateDownloadPdf } from "../../funcs/envelopeEnvelopeCertificateDownloadPdf.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeCertificateDownloadPdfRequest$inboundSchema,
};

export const tool$envelopeEnvelopeCertificateDownloadPdf: ToolDefinition<
  typeof args
> = {
  name: "envelope-envelope-certificate-download-pdf",
  description: `Download envelope certificate PDF

Download the signing certificate for a completed document as a PDF.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopeEnvelopeCertificateDownloadPdf(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
