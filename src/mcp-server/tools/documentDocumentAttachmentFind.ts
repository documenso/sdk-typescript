/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentDocumentAttachmentFind } from "../../funcs/documentDocumentAttachmentFind.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DocumentAttachmentFindRequest$inboundSchema,
};

export const tool$documentDocumentAttachmentFind: ToolDefinition<typeof args> =
  {
    name: "document-document-attachment-find",
    description: `Find attachments

Find all attachments for a document`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await documentDocumentAttachmentFind(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
