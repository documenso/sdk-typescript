/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { foldersFind } from "../../funcs/foldersFind.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.FolderFindFoldersRequest$inboundSchema,
};

export const tool$foldersFind: ToolDefinition<typeof args> = {
  name: "folders-find",
  description: `Find folders

Find folders based on a search criteria`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await foldersFind(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
