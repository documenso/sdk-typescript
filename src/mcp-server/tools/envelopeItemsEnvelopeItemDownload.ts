/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopeItemsEnvelopeItemDownload } from "../../funcs/envelopeItemsEnvelopeItemDownload.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeItemDownloadRequest$inboundSchema,
};

export const tool$envelopeItemsEnvelopeItemDownload: ToolDefinition<
  typeof args
> = {
  name: "envelope-items-envelope-item-download",
  description: `Download an envelope item

Download an envelope item by its ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopeItemsEnvelopeItemDownload(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
