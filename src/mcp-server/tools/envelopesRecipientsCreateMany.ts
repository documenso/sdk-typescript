/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopesRecipientsCreateMany } from "../../funcs/envelopesRecipientsCreateMany.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeRecipientCreateManyRequest$inboundSchema,
};

export const tool$envelopesRecipientsCreateMany: ToolDefinition<typeof args> = {
  name: "envelopes-recipients-create-many",
  description: `Create envelope recipients

Create multiple recipients for an envelope`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopesRecipientsCreateMany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
