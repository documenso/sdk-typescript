/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopeEnvelopeDistribute } from "../../funcs/envelopeEnvelopeDistribute.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeDistributeRequest$inboundSchema,
};

export const tool$envelopeEnvelopeDistribute: ToolDefinition<typeof args> = {
  name: "envelope-envelope-distribute",
  description: `Distribute envelope

Send the envelope to recipients based on your distribution method`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopeEnvelopeDistribute(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
