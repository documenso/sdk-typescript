/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopesAttachmentsDelete } from "../../funcs/envelopesAttachmentsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeAttachmentDeleteRequest$inboundSchema,
};

export const tool$envelopesAttachmentsDelete: ToolDefinition<typeof args> = {
  name: "envelopes-attachments-delete",
  description: `Delete attachment

Delete an attachment from an envelope`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopesAttachmentsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
