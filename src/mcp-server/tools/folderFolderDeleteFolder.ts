/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { folderFolderDeleteFolder } from "../../funcs/folderFolderDeleteFolder.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.FolderDeleteFolderRequest$inboundSchema,
};

export const tool$folderFolderDeleteFolder: ToolDefinition<typeof args> = {
  name: "folder-folder-delete-folder",
  description: `Delete folder

Deletes an existing folder`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await folderFolderDeleteFolder(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
