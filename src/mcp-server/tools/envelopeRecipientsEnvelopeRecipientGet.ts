/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopeRecipientsEnvelopeRecipientGet } from "../../funcs/envelopeRecipientsEnvelopeRecipientGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeRecipientGetRequest$inboundSchema,
};

export const tool$envelopeRecipientsEnvelopeRecipientGet: ToolDefinition<
  typeof args
> = {
  name: "envelope-recipients-envelope-recipient-get",
  description: `Get envelope recipient

Returns an envelope recipient given an ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopeRecipientsEnvelopeRecipientGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
