/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopeRecipientsEnvelopeRecipientCreateMany } from "../../funcs/envelopeRecipientsEnvelopeRecipientCreateMany.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeRecipientCreateManyRequest$inboundSchema,
};

export const tool$envelopeRecipientsEnvelopeRecipientCreateMany: ToolDefinition<
  typeof args
> = {
  name: "envelope-recipients-envelope-recipient-create-many",
  description: `Create envelope recipients

Create multiple recipients for an envelope`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] =
      await envelopeRecipientsEnvelopeRecipientCreateMany(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
