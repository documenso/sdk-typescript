/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { templateTemplateCreateTemplateTemporary } from "../../funcs/templateTemplateCreateTemplateTemporary.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.TemplateCreateTemplateTemporaryRequest$inboundSchema,
};

export const tool$templateTemplateCreateTemplateTemporary: ToolDefinition<
  typeof args
> = {
  name: "template-template-create-template-temporary",
  description: `Create template

You will need to upload the PDF to the provided URL returned. Note: Once V2 API is released, this will be removed since we will allow direct uploads, instead of using an upload URL.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await templateTemplateCreateTemplateTemporary(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
