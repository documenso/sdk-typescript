/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopeAttachmentsEnvelopeAttachmentDelete } from "../../funcs/envelopeAttachmentsEnvelopeAttachmentDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeAttachmentDeleteRequest$inboundSchema,
};

export const tool$envelopeAttachmentsEnvelopeAttachmentDelete: ToolDefinition<
  typeof args
> = {
  name: "envelope-attachments-envelope-attachment-delete",
  description: `Delete attachment

Delete an attachment from an envelope`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopeAttachmentsEnvelopeAttachmentDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
