/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopesRecipientsDelete } from "../../funcs/envelopesRecipientsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeRecipientDeleteRequest$inboundSchema,
};

export const tool$envelopesRecipientsDelete: ToolDefinition<typeof args> = {
  name: "envelopes-recipients-delete",
  description: `Delete envelope recipient

Delete an envelope recipient`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopesRecipientsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
