/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentDocumentCreateDocumentFormData } from "../../funcs/documentDocumentCreateDocumentFormData.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DocumentCreateDocumentFormDataRequest$inboundSchema,
};

export const tool$documentDocumentCreateDocumentFormData: ToolDefinition<
  typeof args
> = {
  name: "document-document-create-document-form-data",
  description: `Create document

Create a document using form data.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentDocumentCreateDocumentFormData(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
