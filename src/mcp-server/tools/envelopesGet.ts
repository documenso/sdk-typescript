/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopesGet } from "../../funcs/envelopesGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeGetRequest$inboundSchema,
};

export const tool$envelopesGet: ToolDefinition<typeof args> = {
  name: "envelopes-get",
  description: `Get envelope

Returns an envelope given an ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopesGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
