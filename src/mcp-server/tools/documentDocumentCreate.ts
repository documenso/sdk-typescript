/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentDocumentCreate } from "../../funcs/documentDocumentCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DocumentCreateRequest$inboundSchema,
};

export const tool$documentDocumentCreate: ToolDefinition<typeof args> = {
  name: "document-document-create",
  description: `Create document

Create a document using form data.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentDocumentCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
