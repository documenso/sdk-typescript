/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentDocumentAttachmentCreate } from "../../funcs/documentDocumentAttachmentCreate.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DocumentAttachmentCreateRequest$inboundSchema,
};

export const tool$documentDocumentAttachmentCreate: ToolDefinition<
  typeof args
> = {
  name: "document-document-attachment-create",
  description: `Create attachment

Create a new attachment for a document`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentDocumentAttachmentCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
