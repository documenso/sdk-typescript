/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopesItemsDelete } from "../../funcs/envelopesItemsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeItemDeleteRequest$inboundSchema,
};

export const tool$envelopesItemsDelete: ToolDefinition<typeof args> = {
  name: "envelopes-items-delete",
  description: `Delete envelope item

Delete an envelope item from an envelope`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopesItemsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
