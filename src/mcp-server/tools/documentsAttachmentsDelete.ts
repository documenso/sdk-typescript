/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentsAttachmentsDelete } from "../../funcs/documentsAttachmentsDelete.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DocumentAttachmentDeleteRequest$inboundSchema,
};

export const tool$documentsAttachmentsDelete: ToolDefinition<typeof args> = {
  name: "documents-attachments-delete",
  description: `Delete attachment

Delete an attachment from a document`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentsAttachmentsDelete(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
