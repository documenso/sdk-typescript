/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentDocumentGetMany } from "../../funcs/documentDocumentGetMany.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DocumentGetManyRequest$inboundSchema,
};

export const tool$documentDocumentGetMany: ToolDefinition<typeof args> = {
  name: "document-document-get-many",
  description: `Get multiple documents

Deprecated: this endpoint is being replaced by the Envelope API. See https://docs.documenso.com/docs/developers/api/migrate-to-envelopes for the migration guide. Retrieve multiple documents by their IDs`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentDocumentGetMany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
