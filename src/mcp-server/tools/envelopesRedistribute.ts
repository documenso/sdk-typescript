/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopesRedistribute } from "../../funcs/envelopesRedistribute.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeRedistributeRequest$inboundSchema,
};

export const tool$envelopesRedistribute: ToolDefinition<typeof args> = {
  name: "envelopes-redistribute",
  description: `Redistribute envelope

Redistribute the envelope to the provided recipients who have not actioned the envelope. Will use the distribution method set in the envelope`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopesRedistribute(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
