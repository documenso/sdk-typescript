/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopesFieldsGet } from "../../funcs/envelopesFieldsGet.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeFieldGetRequest$inboundSchema,
};

export const tool$envelopesFieldsGet: ToolDefinition<typeof args> = {
  name: "envelopes-fields-get",
  description: `Get envelope field

Returns an envelope field given an ID`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopesFieldsGet(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
