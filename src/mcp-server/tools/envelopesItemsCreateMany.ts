/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopesItemsCreateMany } from "../../funcs/envelopesItemsCreateMany.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeItemCreateManyRequest$inboundSchema,
};

export const tool$envelopesItemsCreateMany: ToolDefinition<typeof args> = {
  name: "envelopes-items-create-many",
  description: `Create envelope items

Create multiple envelope items for an envelope`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopesItemsCreateMany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
