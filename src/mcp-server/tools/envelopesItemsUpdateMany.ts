/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopesItemsUpdateMany } from "../../funcs/envelopesItemsUpdateMany.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeItemUpdateManyRequest$inboundSchema,
};

export const tool$envelopesItemsUpdateMany: ToolDefinition<typeof args> = {
  name: "envelopes-items-update-many",
  description: `Update envelope items

Update multiple envelope items for an envelope`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopesItemsUpdateMany(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
