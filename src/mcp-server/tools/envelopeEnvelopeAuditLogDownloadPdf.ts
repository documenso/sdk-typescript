/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { envelopeEnvelopeAuditLogDownloadPdf } from "../../funcs/envelopeEnvelopeAuditLogDownloadPdf.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.EnvelopeAuditLogDownloadPdfRequest$inboundSchema,
};

export const tool$envelopeEnvelopeAuditLogDownloadPdf: ToolDefinition<
  typeof args
> = {
  name: "envelope-envelope-audit-log-download-pdf",
  description: `Download envelope audit log PDF

Download the audit log for a document as a PDF.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await envelopeEnvelopeAuditLogDownloadPdf(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
